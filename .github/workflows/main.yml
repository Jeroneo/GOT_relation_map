name: Build & Push Docker Image

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/jeroneo/got_relation_map:latest

---

### 4. Commit it

Click **"Commit changes"** → commit directly to `main`.

---

### 5. Watch it run

Go back to the **Actions** tab — you'll see the workflow running. Once it's green ✅, your image is live at:
```
ghcr.io/jeroneo/got_relation_map:latest
